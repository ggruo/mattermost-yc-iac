apiVersion: v1
kind: Service
metadata:
  name: notes-alb
  namespace: notes
spec:
  type: NodePort
  selector:
    app: notes
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
