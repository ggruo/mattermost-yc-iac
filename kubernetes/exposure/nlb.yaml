apiVersion: v1
kind: Service
metadata:
  name: notes-public
  namespace: notes
spec:
  type: LoadBalancer
  selector:
    app: notes
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30081
