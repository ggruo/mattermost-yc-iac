---
- name: Install host dependencies
  ansible.builtin.apt:
    name: [python3-venv, python3-pip, ca-certificates, curl, postgresql-client]
    state: present
    update_cache: true
    cache_valid_time: 3600
